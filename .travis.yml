language: python
sudo: required
dist: trusty
cache:
  directories:
    - $HOME/.cache/pip
python: "3.6"

install:
  - "pip install fabric2 requests nose"

script:
  - "nosetests ubio/"

deploy:
  provider: script
  skip_cleanup: true
  script: fab update --envtype=staging
  on:
    branch: master
